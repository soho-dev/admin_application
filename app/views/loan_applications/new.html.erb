<div class="main-content-inner">
  <div class="page-content">
    <div class="page-header">
			<h1>
				Loan Application
				<small>
					<i class="ace-icon fa fa-angle-double-right"></i>
				</small>
			</h1>
		</div>
    <% if params[:address_id].present?  || @loan_application.address_id.present? %>
      <%= render 'form' %>
    <% else %>
      <%= render 'address_eligibility'%>
    <% end %>

  </div>
</div>
